title: "Shenjian's Personal Website"
author:
  name: Shenjian Li
  email: shenjian.li@postgrad.otago.ac.nz

# The `>` after `description:` means to ignore line-breaks until next key.
# If you want to omit the line-break after the end of text, use `>-` instead.
description: >-
 Shenjian is a PhD student with a fully funded scholarship in Finance, interested in Asset Pricing and Machine Learning.

# Build settings

theme: minima

plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Theme-specific settings

minima:
  # Minima date format.
  # Refer to https://shopify.github.io/liquid/filters/date/ if you want to customize this.
  #
   date_format: "%b %-d, %Y"

  # Generate social links in footer.
  #
social_links:
      - { platform: linkedin,       user_url: "https://www.linkedin.com/in/shenjian1996/" }
  #   - { platform: devto,          user_url: "https://dev.to/jekyll" }

# If you want to link only specific pages in your header, uncomment this and add the path to the pages in
# order as they should show up.
#
header_pages:
  - about.md
  - projects.md
  - research.md

# Set to `true` to show excerpts on the homepage.
#
# show_excerpts: false

feed:
  path: "https://github.com/lishenjian1996" 
